function perimetrSquare(x1, y1, x2, y2, x3, y3){
  var A = x1, y2;
  var B = x2, y2;
  var C = x3, y3;
  var AB = Math.sqrt(Math.pow(((x2 - x1) + (y2 - y1)), 2));
  var BC = Math.sqrt(Math.pow(((x3 - x2) + (y3 - y2)), 2));
  var AC = Math.sqrt(Math.pow(((x3 - x1) + (y3 - y1)), 2));
  var P = AB + BC + AC;
  var p = (AB + BC + AC) / 2;
  var S = Math.sqrt(p*(p - AB)*(p - BC)*(p - AC));
  console.log(P);
  console.log(S);
}
console.log(perimetrSquare(2, 3, 4, 5, 6, 9));
